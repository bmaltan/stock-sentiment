{
    "name": "functions",
    "version": "",
    "scripts": {
        "build": "tsc",
        "deploy": "npm i && npm run prestart && func azure functionapp publish invest-track",
        "watch": "tsc --w",
        "prestart": "npm run build && func extensions install",
        "start:host": "func start",
        "local": "npm-run-all --parallel start:host watch",
        "test": "echo \"No tests yet...\""
    },
    "description": "",
    "devDependencies": {
        "@azure/functions": "^1.0.1-beta1",
        "npm-run-all": "^4.1.5",
        "typescript": "^3.3.3"
    },
    "dependencies": {
        "firebase-admin": "^9.4.2"
    }
}
